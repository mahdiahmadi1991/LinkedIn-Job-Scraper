# Idea Inbox Bridge

Canonical backlog and lifecycle tracking are managed in GitHub Project:

- https://github.com/users/mahdiahmadi1991/projects/1

## Intake Rule

Create new work as GitHub Issues using `.github/ISSUE_TEMPLATE/task-intake.yml`.

Optional CLI shortcut for standardized intake:

- `scripts/project-intake.sh`
- `scripts/project-work-branch.sh`

Use direct `gh`/`git` commands for one-step actions; treat scripts as optional convenience only.

Do not use this file as an active backlog ledger.

## Legacy Snapshot (Historical Reference Only)

The former in-repo inbox content was migrated verbatim to:

- https://github.com/mahdiahmadi1991/LinkedIn-Job-Scraper/issues/26
